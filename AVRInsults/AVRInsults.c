
#include "IO.h"
#include <stdio.h>

char* resolveToken( char* token);

int main (void)
{
	initStdInOutUart();

	while(1)
	{	
		printf("You idiot\n"); // Starting simple: the first insult, issued repeatedly.
	}
}

char* resolveToken( char* token)
{
	return NULL;
}